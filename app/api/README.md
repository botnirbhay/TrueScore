# API Routes Placeholder

Use this directory for future App Router API endpoints such as `app/api/analyze/route.ts`.

The current MVP intentionally keeps all logic in local client state, so no route handlers are implemented yet.

